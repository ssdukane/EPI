@using EPi.Commerce.Web.Models.ViewModels
@model EPi.Commerce.Web.Models.Blocks.ViddlerBlock
@{
	string coverImage = "";
	bool hasCoverImage = Model.HasCoverImage;
	if (hasCoverImage)
	{
		coverImage = Url.ContentUrl(Model.CoverImage);
	}
}
@if (Model.HasVideo)
{
	@Html.Partial("_Video", new VideoViewModel()
{
	CoverImageUrl = coverImage,
	HasCoverImage = hasCoverImage,
	IframeUrl = Model.ViddlerVideo.GetIframeUrl(hasCoverImage),
	IframeId = "viddler-" + Model.ViddlerVideo.Id
})
}